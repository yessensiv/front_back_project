# 3 және 4-тапсырмалар: түсіндірме

Бұл файл сайтқа қосылған 3 және 4-тапсырмалардың жұмысын түсіндіреді. Екі тапсырма да негізгі жобаның ішіне жеке **вкладка** ретінде қосылды.

## Қай файлдар өзгертілді

| Файл | Не үшін өзгертілді |
| --- | --- |
| `index.html` | 3 және 4-тапсырмаға арналған вкладкалар, батырмалар, енгізу өрістері және мәтіндер қосылды. |
| `script.js` | Кестені құру, ұяшықтарды бояу, боялған ұяшықтарды санау және тақырыпты ауыстыру логикасы жазылды. |
| `styles.css` | Кесте, батырмалар, 3–4-тапсырма блоктары және ашық/қараңғы тақырып стильдері қосылды. |

---

# 3-тапсырма — Динамикалық кесте

## Мақсаты

Пайдаланушы жол мен баған санын енгізеді. Бағдарлама осы сандар бойынша кесте жасайды. Кестедегі кез келген ұяшықты басқанда ол жасыл түске боялады. Қайта басқанда бояуы өшеді. Төменде боялған ұяшықтар саны көрсетіледі.

Мысалы:

- Жол саны: `3`
- Баған саны: `4`
- Нәтиже: 3 жолдан және 4 бағаннан тұратын кесте пайда болады.

## HTML бөлігі

`index.html` файлына 3-тапсырмаға арналған вкладка қосылды:

```html
<button class="tab-btn" onclick="switchTab('task3')" id="tab-btn-task3" type="button">
  Тапсырма 3
</button>
```

Мұнда:

- `onclick="switchTab('task3')"` — батырма басылғанда 3-тапсырма вкладкасын ашады;
- `id="tab-btn-task3"` — JavaScript осы батырманы тауып, оны белсенді ете алады;
- `type="button"` — бұл батырма форма жібермейтінін көрсетеді;
- вкладкалардың барлығы бірдей форматта жазылған: `Тапсырма 1`, `Тапсырма 2`, `Тапсырма 3`, `Тапсырма 4`.

Вкладканың негізгі бөлігі:

```html
<section id="view-task3" class="tab-view task-view">
```

`id="view-task3"` атауы `switchTab('task3')` функциясымен байланысты. JavaScript `view-task3` блогына `active` класын қосқанда ол экранда көрінеді.

### Енгізу өрістері

```html
<input type="number" id="rows-input" value="4" min="1" max="20">
<input type="number" id="cols-input" value="4" min="1" max="20">
```

- `rows-input` — жол санын енгізу өрісі;
- `cols-input` — баған санын енгізу өрісі;
- `value="4"` — сайт ашылғанда кесте әдепкі бойынша 4×4 болады;
- `min="1"` және `max="20"` — бірден 20-дан артық жол немесе баған бермеуге көмектеседі.

Кестені құратын батырма:

```html
<button id="build-table-btn" type="button">Кесте құру</button>
```

Боялған ұяшықтар саны шығатын орын:

```html
<p id="colored-counter">Боялған ұяшықтар саны: 0</p>
```

Ал кестенің өзі мына бос блокқа JavaScript арқылы қосылады:

```html
<div id="table-container"></div>
```

## JavaScript бөлігі

Алдымен HTML элементтерін айнымалыларға сақтаймыз:

```javascript
const zholEngizu = document.getElementById("rows-input");
const baganEngizu = document.getElementById("cols-input");
const kesteBatyrmasy = document.getElementById("build-table-btn");
const kesteOrny = document.getElementById("table-container");
const sanaqMati = document.getElementById("colored-counter");
```

Айнымалы атауларының мағынасы:

| Айнымалы | Мағынасы |
| --- | --- |
| `zholEngizu` | Жол санын енгізетін өріс. |
| `baganEngizu` | Баған санын енгізетін өріс. |
| `kesteBatyrmasy` | «Кесте құру» батырмасы. |
| `kesteOrny` | Жаңа кесте шығатын орын. |
| `sanaqMati` | Боялған ұяшықтар саны жазылатын мәтін. |

### Боялған ұяшықтарды санау

```javascript
function boyaulySana() {
  const boyaulyUyashyktar = document.querySelectorAll(".colored");
  sanaqMati.textContent = "Боялған ұяшықтар саны: " + boyaulyUyashyktar.length;
}
```

Функция барлық `.colored` классы бар ұяшықтарды іздейді. `querySelectorAll()` оларды тізім ретінде қайтарады, ал `.length` сол тізімдегі элемент санын береді. Соңында сан экрандағы мәтінге жазылады.

### Кестені құру

```javascript
function kesteKuru() {
  const zholSany = zholEngizu.value;
  const baganSany = baganEngizu.value;
  kesteOrny.innerHTML = "";
```

1. `zholSany` және `baganSany` — пайдаланушы енгізген сандар.
2. `kesteOrny.innerHTML = ""` — бұрын жасалған кестені өшіреді. Сондықтан жаңа өлшем енгізіп, қайтадан батырманы басқанда ескі кесте қалмайды.

Жаңа `table` тегі жасалады:

```javascript
const keste = document.createElement("table");
keste.className = "dynamic-table";
```

`dynamic-table` CSS класын қосу кестеге стиль береді.

Екі `for` циклі кестені жасайды:

```javascript
for (let zhol = 0; zhol < zholSany; zhol++) {
  const kesteZholy = document.createElement("tr");

  for (let bagan = 0; bagan < baganSany; bagan++) {
    const uyashyk = document.createElement("td");
    uyashyk.textContent = (zhol + 1) + ":" + (bagan + 1);
    kesteZholy.appendChild(uyashyk);
  }

  keste.appendChild(kesteZholy);
}
```

- Сыртқы цикл бір жол жасайды: `<tr>`.
- Ішкі цикл сол жолдың ішіне ұяшықтар жасайды: `<td>`.
- `zhol + 1` және `bagan + 1` нөмірлеуді 1-ден бастау үшін қажет.
- Мысалы, бірінші ұяшықта `1:1`, келесісінде `1:2` мәтіні шығады.
- `appendChild()` элементті басқа элементтің ішіне қосады.

### Ұяшықты бояу

Әр ұяшыққа басу оқиғасы жазылады:

```javascript
uyashyk.onclick = function () {
  uyashyk.classList.toggle("colored");
  boyaulySana();
};
```

`classList.toggle("colored")` екі түрлі әрекет жасайды:

- егер `colored` класы жоқ болса — оны қосады;
- егер класс бар болса — оны өшіреді.

Класс қосылған кезде CSS ұяшықты жасыл етеді. Әр басудан кейін `boyaulySana()` функциясы іске қосылып, сан жаңартылады.

Соңында кестені бетке шығарамыз:

```javascript
kesteOrny.appendChild(keste);
boyaulySana();
```

### Батырмаға функцияны бекіту

```javascript
kesteBatyrmasy.onclick = kesteKuru;
kesteKuru();
```

Бірінші жол «Кесте құру» батырмасына функцияны бекітеді. Екінші жол сайт жүктелген сәтте бірден 4×4 кесте көрсету үшін жазылған.

## CSS бөлігі

Маңызды стильдер:

```css
.dynamic-table td {
  width: 58px;
  height: 50px;
  border: 1px solid var(--border-subtle);
  color: var(--text-main);
  text-align: center;
  cursor: pointer;
}

.dynamic-table td.colored {
  color: #fff;
  background: #10b981;
  border-color: #34d399;
}
```

`.dynamic-table td` — барлық ұяшықтардың өлшемін, жиегін және курсорын баптайды. `.dynamic-table td.colored` — JavaScript қосатын `colored` класы бар ұяшықтың жасыл стилі.

---

# 4-тапсырма — Тақырыпты ауыстыру

## Мақсаты

Батырманы басқанда сайт ашық және қараңғы тақырып арасында ауысады. Тақырып бүкіл `body` элементіне әсер етеді, сондықтан сайттың барлық бөлігі өзгереді.

## HTML бөлігі

4-тапсырмаға арналған вкладка:

```html
<button class="tab-btn" onclick="switchTab('task4')" id="tab-btn-task4" type="button">
  Тапсырма 4
</button>
```

Тақырыпты ауыстыратын батырма:

```html
<button id="theme-toggle-btn" type="button">Ашық тақырыпты қосу</button>
```

Тақырып күйін көрсететін мәтін:

```html
<p id="theme-status">Қазір қараңғы тақырып қосулы.</p>
```

## JavaScript бөлігі

Алдымен элементтерді табамыз:

```javascript
const temaBatyrmasy = document.getElementById("theme-toggle-btn");
const temaMati = document.getElementById("theme-status");
```

Батырма басылғандағы код:

```javascript
temaBatyrmasy.onclick = function () {
  document.body.classList.toggle("light-theme");

  if (document.body.classList.contains("light-theme")) {
    temaMati.textContent = "Қазір ашық тақырып қосулы.";
    temaBatyrmasy.textContent = "Қараңғы тақырыпты қосу";
  } else {
    temaMati.textContent = "Қазір қараңғы тақырып қосулы.";
    temaBatyrmasy.textContent = "Ашық тақырыпты қосу";
  }
};
```

Жұмыс реті:

1. `document.body.classList.toggle("light-theme")` — `body` тегіне `light-theme` класын қосады немесе өшіреді.
2. Егер `light-theme` класы бар болса, ашық тақырып қосулы. Мәтін және батырма атауы соған сай өзгереді.
3. Егер класс жоқ болса, қараңғы тақырып қосулы. Мәтін және батырма атауы қайта өзгереді.

## CSS бөлігі

Сайттың негізгі түстері CSS айнымалыларында жазылған:

```css
:root {
  --bg-dark: #090d16;
  --text-main: #f8fafc;
  --text-muted: #94a3b8;
}
```

Әдепкіде қараңғы тақырып қолданылады. `light-theme` класы қосылғанда айнымалылардың мәні ауысады:

```css
body.light-theme {
  --bg-dark: #f1f5f9;
  --bg-card: rgba(255, 255, 255, 0.88);
  --text-main: #0f172a;
  --text-muted: #475569;
}
```

Себебі басқа стильдерде `var(--text-main)` және `var(--text-muted)` пайдаланылады, сол стильдер автоматты түрде жаңа түске өтеді.

### Қараңғы тақырыптағы мәтіннің көрінуі

Қараңғы фондағы мәтін оқылатын болуы үшін арнайы ереже қосылды:

```css
body:not(.light-theme) .content-box,
body:not(.light-theme) .task-heading h2,
body:not(.light-theme) .theme-demo-card h3,
body:not(.light-theme) .dynamic-table td {
  color: #f8fafc;
}
```

Бұл код `light-theme` класы жоқ кезде, яғни қараңғы режимде, мәтінді ашық түске бояйды. Сондықтан әріптер қара фонмен араласып кетпейді.

Вкладка мәтіндері де екі тақырыпта анық көріну үшін бөлек бапталды:

```css
body:not(.light-theme) .tab-btn { color: #e2e8f0; }
body.light-theme .tab-btn { color: #1e293b; }
```

---

# Вкладкаларды ауыстыру

Барлық вкладкаларды бұрыннан бар `switchTab()` функциясы басқарады. DOM тапсырмалары (1–4) үшін функцияға ортақ шарт қосылды:

```javascript
else if (["task1", "task2", "task3", "task4"].includes(target)) {
  document.getElementById("tab-btn-" + target).classList.add("active");
  document.getElementById("view-" + target).classList.add("active");
}
```

`includes(target)` берілген `target` атауы осы тізімде бар-жоғын тексереді. Мысалы, `switchTab('task3')` іске қосылса:

1. `tab-btn-task3` батырмасына `active` класы қосылады;
2. `view-task3` бөліміне `active` класы қосылады;
3. CSS-тегі `.tab-view.active { display: block; }` ережесі блокты экранға шығарады.

---

# Қорғауда қалай түсіндіруге болады

Қысқаша айтуға болатын нұсқа:

> 3-тапсырмада пайдаланушы жол мен баған санын енгізеді. JavaScript екі цикл арқылы HTML кестесін жасайды. Әр ұяшықты басқанда `colored` класы қосылады, CSS сол ұяшықты жасыл түске бояйды. `querySelectorAll('.colored')` арқылы боялған ұяшықтарды санап, санын экранға шығарамын.

> 4-тапсырмада батырма `body` тегіне `light-theme` класын қосады немесе алып тастайды. CSS айнымалылары фон мен мәтін түстерін өзгертеді. Батырма атауы мен тақырып күйі де JavaScript арқылы жаңарады.

---

# Командалар мен кластардың толық анықтамалығы

Бұл бөлімдегі түсіндірмелер 3 және 4-тапсырмаларда нақты қолданылған командаларға арналған.

## DOM деген не?

**DOM (Document Object Model)** — браузер HTML бетті объектілер ағашы ретінде көрсететін модель. JavaScript сол ағаштағы элементтерді таба, өзгерте, қоса және жоя алады.

Мысалы, HTML-дегі мына элемент:

```html
<div id="table-container"></div>
```

JavaScript үшін `table-container` деген объект болады. Сол объектінің ішіне кесте қосылады.

## `document.getElementById()`

Мысал:

```javascript
const kesteOrny = document.getElementById("table-container");
```

Бұл команда `id="table-container"` бар **бір элементті** іздейді және қайтарады.

- `document` — ашық HTML құжаты;
- `getElementById` — ID арқылы элементті табу әдісі;
- `"table-container"` — HTML-дегі ID атауы;
- `const kesteOrny` — табылған элементті сақтайтын айнымалы.

Бұл жерде `kesteOrny` — кесте көрсетілетін `div`.

## `document.querySelectorAll()` — толық түсіндірме

Код:

```javascript
const boyaulyUyashyktar = document.querySelectorAll(".colored");
```

`querySelectorAll()` — CSS селекторы бойынша **барлық сәйкес элементтерді** іздейтін команда.

Осы жол қалай жұмыс істейді:

1. `document` — бүкіл беттен іздеу керек екенін білдіреді.
2. `querySelectorAll(...)` — «барлығын таңда» деген әдіс.
3. `".colored"` — CSS селекторы. Нүкте (`.`) бұл атау класс екенін көрсетеді.
4. Нәтижеде `.colored` класы бар барлық элементтердің тізімі шығады.
5. Бұл тізім `boyaulyUyashyktar` айнымалысына сақталады.

Мысалы, мынадай үш ұяшық боялса:

```html
<td class="colored">1:1</td>
<td class="colored">1:2</td>
<td class="colored">2:3</td>
```

онда `document.querySelectorAll(".colored")` осы үш `td` элементтен тұратын тізімді береді. `boyaulyUyashyktar.length` мәні `3` болады.

`querySelectorAll()` **бір элемент емес, тізім** қайтарады. Сондықтан оны санауға (`.length`) немесе циклмен қарап шығуға болады. Ал тек бір элемент қажет болғанда `document.querySelector(".colored")` немесе `getElementById()` қолданылады.

## `.length`

```javascript
boyaulyUyashyktar.length
```

`.length` — тізімнің ұзындығын, яғни ондағы элементтер санын қайтарады. Осы тапсырмада ол боялған ұяшықтар санын береді.

## `document.createElement()`

Мысалдар:

```javascript
const keste = document.createElement("table");
const kesteZholy = document.createElement("tr");
const uyashyk = document.createElement("td");
```

`createElement()` браузер жадында жаңа HTML элементін жасайды, бірақ оны бірден бетке шығармайды.

- `"table"` — кесте;
- `"tr"` — кестенің жолы;
- `"td"` — кестенің бір ұяшығы.

Элемент экранда көрінуі үшін оны кейін `appendChild()` арқылы басқа элементтің ішіне қосу керек.

## `appendChild()`

Мысал:

```javascript
kesteZholy.appendChild(uyashyk);
keste.appendChild(kesteZholy);
kesteOrny.appendChild(keste);
```

`appendChild()` — бір элементті екінші элементтің соңына қосады.

Осы тапсырмадағы реті:

1. `uyashyk` (`td`) жолдың ішіне қосылады;
2. `kesteZholy` (`tr`) кестенің ішіне қосылады;
3. `keste` (`table`) контейнер `div` ішіне қосылады;
4. Содан кейін кесте браузер бетінде көрінеді.

## `innerHTML = ""`

```javascript
kesteOrny.innerHTML = "";
```

`innerHTML` элементтің ішіндегі HTML мазмұнын білдіреді. Бос жол (`""`) беру контейнердің ішін толық тазалайды.

Бұл команда не үшін қажет:

- пайдаланушы жаңа жол мен баған санын енгізгенде;
- ескі кесте өшеді;
- оның орнына тек жаңа кесте шығады;
- бірнеше кесте бірінің астына бірі жиналмайды.

## `textContent`

Мысал:

```javascript
uyashyk.textContent = (zhol + 1) + ":" + (bagan + 1);
```

`textContent` элемент ішіндегі жай мәтінді орнатады. Мұнда ұяшыққа оның координатасы жазылады: `1:1`, `1:2`, `2:1` және т.б.

Тағы бір мысал:

```javascript
sanaqMati.textContent = "Боялған ұяшықтар саны: " + boyaulyUyashyktar.length;
```

Бұл жол санақ мәтінін және нақты санды біріктіріп, экранда көрсетеді.

## `for` циклі

```javascript
for (let zhol = 0; zhol < zholSany; zhol++)
```

Бұл цикл кестенің жолдарын жасайды.

- `let zhol = 0` — санау 0-ден басталады;
- `zhol < zholSany` — енгізілген жол санына жеткенше цикл жалғасады;
- `zhol++` — әр айналымнан кейін `zhol` 1-ге артады.

Ішкі цикл бағандарды жасайды. Екі цикл бірге болғандықтан, әр жолға қажетті ұяшық саны қосылады.

Мысалы, жол саны 2, баған саны 3 болса:

```text
сыртқы цикл: 2 рет  →  2 жол
ішкі цикл:   әр жолда 3 рет  →  3 ұяшық
нәтиже: 2 × 3 = 6 ұяшық
```

## `onclick`

```javascript
uyashyk.onclick = function () {
  uyashyk.classList.toggle("colored");
  boyaulySana();
};
```

`onclick` — элементке басу оқиғасын бекітеді. Пайдаланушы дәл сол ұяшықты басқанда, жақша ішіндегі функция орындалады.

Бұл жерде функция екі жұмыс істейді:

1. ұяшықтың `colored` класын ауыстырады;
2. боялған ұяшықтар санын қайта есептейді.

## `classList` және `classList.toggle()`

Әр HTML элементтің класс тізімі болады. `classList` сол тізіммен жұмыс істеуге арналған.

```javascript
uyashyk.classList.toggle("colored");
```

`toggle()` класты ауыстырады:

- `colored` жоқ болса, оны қосады;
- `colored` бар болса, оны жояды.

Осыдан кейін CSS ережесі іске қосылады:

```css
.dynamic-table td.colored {
  background: #10b981;
  color: #fff;
}
```

Яғни JavaScript түсті тікелей орнатпайды. Ол тек `colored` класын қосады, ал CSS сол класс бар кезде ұяшықты жасыл түске бояйды. Бұл тәсіл кодты реттірек етеді.

## `className`

```javascript
keste.className = "dynamic-table";
```

`className` элементке класс атауын жазады. Осы жол жаңа кестеге `dynamic-table` класын береді. Сондықтан `.dynamic-table td` CSS стилі осы кестенің ұяшықтарына қолданылады.

## `classList.contains()`

```javascript
document.body.classList.contains("light-theme")
```

`contains()` сұрақ қояды: «Элементте осы класс бар ма?» Нәтиже тек екі түрлі болады:

- `true` — класс бар;
- `false` — класс жоқ.

4-тапсырмада бұл команда қазір ашық тақырып қосулы ма, жоқ па соны анықтайды.

## `document.body.classList.toggle("light-theme")`

`document.body` — беттің негізгі `body` тегі. Бұл команда оның кластар тізіміндегі `light-theme` класын қосады немесе өшіреді.

- класс қосылса — CSS-тегі `body.light-theme { ... }` ережелері қолданылады, ашық тақырып шығады;
- класс өшірілсе — бастапқы қараңғы түстер қалады.

## `if ... else`

```javascript
if (document.body.classList.contains("light-theme")) {
  // ашық тақырып
} else {
  // қараңғы тақырып
}
```

`if` — шартты тексеру. Егер шарт дұрыс болса (`true`), бірінші блок орындалады. Дұрыс болмаса (`false`), `else` блогы орындалады.

Бұл жерде ол батырма мен хабарлама мәтінін ағымдағы тақырыпқа сай ауыстырады.

---

# CSS кластарын толық түсіндіру

| Класс | Қай жерде қолданылады | Не істейді |
| --- | --- | --- |
| `.tab-btn` | Барлық вкладка батырмаларында | Өлшемін, фонын, жиегін және мәтін түсін береді. |
| `.active` | Белсенді вкладкада және көрінетін бөлімде | Вкладканы бояйды; `.tab-view.active` бөлімді экранға шығарады. |
| `.tab-view` | Әр вкладка бөлімінде | Бөлімді әдепкіде жасырады: `display: none`. |
| `.task-view` | 1–4 тапсырма бөлімдерінде | Тапсырма блогының ең үлкен енін шектейді. |
| `.task-heading` | Тапсырма атауы бар блокта | Тақырып пен келесі блок арасына бос орын жасайды. |
| `.table-controls` | 3-тапсырмадағы өрістер мен батырмада | Элементтерді бір жолға жинайды, фон және жиек береді. |
| `.dynamic-table` | JavaScript жасайтын `table` тегінде | Кесте ұяшықтарының арақашықтығын реттейді. |
| `.colored` | Басылған кесте ұяшығында | Ұяшықты жасыл түске бояйды. |
| `.colored-counter` | Боялған ұяшықтар санының мәтінінде | Санақ мәтінінің арақашықтығы мен түсін береді. |
| `.theme-demo-card` | 4-тапсырмадағы тақырып блогында | Тақырып батырмасы мен мәтінін бір карточкаға орналастырады. |
| `.light-theme` | `body` тегінде | Ашық тақырыптың CSS айнымалыларын қосады. Класс жоқ болса, қараңғы тақырып қалады. |
| `.practice-nav` | «ПРАКТИКА DOM» навигациясында | 1–4 тапсырманың вкладкаларын бір топ ретінде стильдейді. |

## CSS селекторын оқу

```css
.dynamic-table td.colored
```

Бұл селектор: «`dynamic-table` класы бар кестенің ішінде орналасқан және `colored` класы бар барлық `td` ұяшықтарына стиль қолдан» дегенді білдіреді.

```css
body:not(.light-theme) .practice-nav .tab-btn
```

Бұл: «егер `body` элементінде `light-theme` класы жоқ болса, `.practice-nav` ішіндегі `.tab-btn` батырмаларына осы стильді қолдан» дегенді білдіреді. Яғни бұл — қараңғы тақырыптағы DOM-вкладкалар стилі.

## Неліктен түстер CSS-те, ал әрекет JavaScript-те?

JavaScript пайдаланушы әрекетіне жауап береді: батырманы басу, класс қосу, мәтінді өзгерту. CSS элемент қалай көрінетінін басқарады: түс, фон, жиек, өлшем. Осы екі жауапкершілікті бөлу кодты түсінуге және өзгертуге ыңғайлы етеді.
